import { TalkingListApplication } from './talking-list-application';

describe('TalkingListApplication', () => {
  it('should create an instance', () => {
    expect(new TalkingListApplication()).toBeTruthy();
  });
});
