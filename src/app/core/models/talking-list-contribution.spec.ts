import { TalkingListContribution } from './talking-list-contribution';

describe('TalkingListContribution', () => {
  it('should create an instance', () => {
    expect(new TalkingListContribution()).toBeTruthy();
  });
});
