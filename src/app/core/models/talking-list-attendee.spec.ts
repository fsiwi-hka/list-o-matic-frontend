import { TalkingListAttendee } from './talking-list-attendee';

describe('TalkingListAttendee', () => {
  it('should create an instance', () => {
    expect(new TalkingListAttendee()).toBeTruthy();
  });
});
